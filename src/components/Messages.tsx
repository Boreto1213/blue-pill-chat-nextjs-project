import { FC } from 'react'

interface MessagesProps {
  
}

const Messages: FC<MessagesProps> = ({}) => {
  return <div id='messages' className='flex h-full flex-1 flex-col-reverse'>Messages</div>
}

export default Messages